J’ai vérifié l’étape 1 et 2 du projet FutureKawa.

Avant de passer à MQTT ou aux alertes, je veux corriger et stabiliser l’étape 2.

Merci de faire uniquement les corrections suivantes :

1. Garder comme backend principal uniquement :
   backend-country/brazil-service

2. Supprimer ou ignorer le dossier :
   backend-brazil
   car il crée une confusion avec le backend principal.

3. Modifier prisma/schema.prisma pour utiliser des enums Prisma au lieu de simples String :

enum LotStatus {
CONFORME
EN_ALERTE
PERIME
}

enum AlertType {
TEMPERATURE
HUMIDITY
EXPIRED_LOT
}

Le modèle CoffeeLot doit utiliser :
status LotStatus @default(CONFORME)

Le modèle Alert doit utiliser :
type AlertType

4. Ajouter un fichier .env.example avec :

DATABASE_URL="postgresql://futurekawa:futurekawa123@localhost:5432/futurekawa_brazil"
PORT=3001

5. Remettre .env dans .gitignore.

6. Ajouter une courte section README pour expliquer comment lancer l’étape 2 :

* docker compose up -d postgres-brazil mosquitto-brazil
* cd backend-country/brazil-service
* npm install
* npx prisma migrate dev --name init
* npm run start:dev

7. Ne pas ajouter MQTT, alertes avancées, backend central ou frontend pour l’instant.
   Je veux seulement valider proprement le backend Brésil avec les routes de lots.
