Maintenant on passe à l’étape 2 du projet FutureKawa.

Contexte :
Je travaille sur une MSPR FutureKawa. Le projet consiste à créer une solution applicative de suivi des stocks de café vert et des conditions de stockage. Pour le prototype, on développe complètement un backend pays exemple : Brésil.

Stack choisie :

* Backend : NestJS
* Base de données : PostgreSQL
* ORM : Prisma
* Docker : Docker Compose
* Langage : TypeScript

Important :
Ne fais pas tout le projet maintenant. Je veux uniquement l’étape 2.

Objectif de l’étape 2 :
Créer le backend NestJS du pays Brésil avec Prisma et les premières routes API pour gérer les lots de café.

Structure attendue :
Le backend doit être dans :

backend-country/brazil-service

Ce que je veux que tu me génères :

1. Les commandes pour créer le projet NestJS :

   * installation NestJS CLI si nécessaire
   * création du projet brazil-service
   * installation Prisma et @prisma/client
   * initialisation Prisma

2. La configuration Prisma avec PostgreSQL :

   * fichier .env avec DATABASE_URL
   * fichier prisma/schema.prisma

3. Les modèles Prisma suivants :

CoffeeLot :

* id
* lotCode unique
* country
* warehouseName
* storageDate
* status : CONFORME, EN_ALERTE, PERIME
* createdAt

SensorMeasurement :

* id
* warehouseName
* temperature
* humidity
* measuredAt

Alert :

* id
* type : TEMPERATURE, HUMIDITY, EXPIRED_LOT
* message
* status
* createdAt

4. Générer le module Prisma dans NestJS :

   * prisma.module.ts
   * prisma.service.ts

5. Générer le module Lots :

   * lots.module.ts
   * lots.service.ts
   * lots.controller.ts

6. Créer les routes API suivantes :

POST /api/lots
Permet de créer un lot.

GET /api/lots
Permet d’afficher tous les lots.

GET /api/lots/fifo
Permet d’afficher les lots triés par date de stockage, du plus ancien au plus récent.

7. Donner un exemple de body JSON pour tester POST /api/lots :

{
"lotCode": "BR-LOT-001",
"country": "Brazil",
"warehouseName": "Warehouse Brazil 1",
"storageDate": "2025-01-10"
}

8. Donner les commandes pour :

   * lancer PostgreSQL avec Docker Compose
   * lancer la migration Prisma
   * lancer le backend NestJS
   * tester les routes

9. Explique-moi brièvement le rôle de chaque fichier créé, parce que je dois pouvoir l’expliquer au jury.

Ne passe pas encore à MQTT, aux alertes, au backend central ou au frontend. On fera ça après.
