On passe maintenant à l’étape 5 du projet FutureKawa.

Le backend Brésil est déjà terminé et push sur GitHub.

Il contient :

* gestion des lots
* tri FIFO
* mesures température/humidité
* alertes température/humidité
* vérification des lots périmés
* intégration MQTT avec Mosquitto
* stockage PostgreSQL avec Prisma

Objectif de l’étape 5 :
Créer le backend central “siège” avec NestJS.

Structure attendue :
Créer le backend central dans :

backend-central/

Stack :

* NestJS
* TypeScript
* Axios ou HttpModule NestJS

Important :
Ne pas créer le frontend maintenant.
Ne pas ajouter Jenkins maintenant.
Ne pas modifier le backend Brésil sauf si c’est strictement nécessaire.

Variables d’environnement :
PORT=3002
BRAZIL_SERVICE_URL="http://localhost:3001"

Routes à créer :

GET /api/central/countries
Retourne :

* Brazil
* Ecuador
* Colombia

GET /api/central/brazil/lots
Appelle :
GET http://localhost:3001/api/lots

GET /api/central/brazil/lots/fifo
Appelle :
GET http://localhost:3001/api/lots/fifo

GET /api/central/brazil/measurements
Appelle :
GET http://localhost:3001/api/measurements

GET /api/central/brazil/alerts
Appelle :
GET http://localhost:3001/api/alerts

Fichiers attendus :

* src/central/central.module.ts
* src/central/central.service.ts
* src/central/central.controller.ts

Mettre à jour :

* app.module.ts
* .env.example
* README.md

Ajouter une gestion d’erreur :
Si le backend Brésil ne répond pas, retourner un message clair :
"Brazil service is unavailable"

Donne aussi les commandes pour tester avec PowerShell :

Invoke-RestMethod -Uri "http://localhost:3002/api/central/countries" -Method Get

Invoke-RestMethod -Uri "http://localhost:3002/api/central/brazil/lots" -Method Get

Invoke-RestMethod -Uri "http://localhost:3002/api/central/brazil/measurements" -Method Get

Invoke-RestMethod -Uri "http://localhost:3002/api/central/brazil/alerts" -Method Get

Explique brièvement le rôle de chaque fichier créé.
